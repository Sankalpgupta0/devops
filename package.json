{
  "name": "todo_app",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "sankalp gupta",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "backend": "cd backend && nodemon index.js",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"cd backend && nodemon index.js\" \"cd frontend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "concurrently \"cd backend && npm run lint\" \"cd frontend && npm run lint\"",
    "format": "concurrently \"cd backend && nmp run format\" \"cd frontend && npm run format\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
